# Variables de entorno – BiblioMercado
# Copia este archivo como .env y reemplaza la URL cuando tengas el backend real.

# URL base del backend (AWS, Railway, Render, etc.)
# Ejemplo: https://tu-api.execute-api.us-east-1.amazonaws.com/prod
VITE_API_BASE_URL=http://localhost:3000
